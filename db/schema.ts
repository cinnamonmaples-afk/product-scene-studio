import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const assets=sqliteTable('assets',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),mime:text('mime').notNull(),created:integer('created').notNull()});
export const collections=sqliteTable('collections',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),config:text('config').notNull(),created:integer('created').notNull()},t=>[index('collections_owner_created').on(t.owner,t.created)]);
export const jobs=sqliteTable('jobs',{id:text('id').primaryKey(),collection:text('collection_id').notNull(),owner:text('owner').notNull(),position:integer('position').notNull(),prompt:text('prompt').notNull(),status:text('status').notNull().default('pending'),image:text('image'),error:text('error'),started:integer('started'),attempt:text('attempt')},t=>[index('jobs_collection_position').on(t.collection,t.position),index('jobs_owner_status').on(t.owner,t.status)]);
export const settings=sqliteTable('settings',{owner:text('owner').primaryKey(),secret:text('secret').notNull(),model:text('model').notNull(),provider:text('provider').notNull().default('gemini'),doubaoSecret:text('doubao_secret'),doubaoModel:text('doubao_model').notNull().default('doubao-seedream-5-0-pro-260628'),geminiCooldown:integer('gemini_cooldown').notNull().default(0),doubaoCooldown:integer('doubao_cooldown').notNull().default(0)});
